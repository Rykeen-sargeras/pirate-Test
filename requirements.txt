# Savage Loyalty Test

Files:
- `main.py` — Flask application
- `background.png` — responsive full-screen background
- `requirements.txt` — Python dependencies

Run locally:
```bash
pip install -r requirements.txt
python main.py
```
Then open http://127.0.0.1:5000
